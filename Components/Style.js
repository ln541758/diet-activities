import { StyleSheet, Text, View } from "react-native";


const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#CDC3E2",
    alignItems: "center",
    justifyContent: "center",
  },
});

export default styles.container;
